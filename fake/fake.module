<?php

/*
	implementation of hook_nodeapi
*/
function fake_nodeapi(&$node, $op, $a3 = NULL, $a4 = NULL) {
  switch ($op) {
  }
}

/*
	implementation of hook_user

*/
function fake_user($op, &$edit, &$account, $category = NULL) {
  switch ($op) {

  }
}

function fake_views_api() {
  return array(
    'api' => 2,
    'path' => drupal_get_path('module', 'fake'),
  );
}

